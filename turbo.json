{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist", ".output", "build"]
    },
    "dev": { "dependsOn": ["^dev"] },
    "lint": {},
    "format": {},
    "type-check": {},
    "release": {
      "dependsOn": ["^release"]
    }
  }
}
